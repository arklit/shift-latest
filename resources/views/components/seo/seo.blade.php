<div class="container">
    <div class="seo">
        @if(!empty($seo_title))
            <h3 class="title">{{$seo_title}}</h3>
       @endif
        <p class="text">123</p>
        @if(!empty($image))
            <img src="{{$image}}" alt="seo">
       @endif
    </div>
</div>
